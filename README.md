# Virtual Try-On Website

A lightweight browser app where you can:

- Upload your own photo.
- Upload a cloth/model image.
- Drag, resize, rotate, and adjust cloth opacity.
- Download the final virtual try-on output.

## Run locally

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173` in your browser.
